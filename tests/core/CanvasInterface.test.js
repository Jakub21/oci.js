import {default as CanvasInterface} from '../../source/core/CanvasInterface.js'

describe('core.CanvasInterface', () => {
  test('class exists', () => {
    expect(CanvasInterface.constructor).toBeDefined();
  });
});
