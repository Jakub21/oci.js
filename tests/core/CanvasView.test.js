import {default as CanvasView} from '../../source/core/CanvasView.js'

describe('core.CanvasView', () => {
  test('class exists', () => {
    expect(CanvasView.constructor).toBeDefined();
  });
});
