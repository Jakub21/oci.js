import {default as ElementStore} from '../../source/core/ElementStore.js'

describe('core.ElementStore', () => {
  test('class exists', () => {
    expect(ElementStore.constructor).toBeDefined();
  });
});
