import {default as Transform} from '../../source/core/Transform.js'

describe('core.Transform', () => {
  test('class exists', () => {
    expect(Transform.constructor).toBeDefined();
  });
});
